// ...existing code...

// Change default action from run to edit
function handleAliasSelection(alias) {
  // Instead of running the alias directly, open it for editing
  editAlias(alias);
}

// ...existing code...
